out
dist
node_modules
.vscode-test/
*.vsix
.DS_Store
coverage/

# Mutation testing (Stryker) and Scorecard SARIF output
reports/
.stryker-tmp/
results.sarif
.env
.env.*

# User-specific extension config (generated by "Configure Preview" command)
.json-schema-preview-config.json

# Playwright e2e — raw screenshot frames (GIFs in docs/public/ are the output)
screenshots/
playwright.e2e.config.js
playwright.e2e.config.js.map

# demo-showcase-mouse's raw ffmpeg/xdotool screen recording (the GIF in
# docs/public/ is the output — see scripts/make-showcase-gif.mjs)
videos/

# Playwright state
playwright-report/
test-results/

# Docs (VitePress)
docs/node_modules
docs/.vitepress/dist
docs/.vitepress/cache
